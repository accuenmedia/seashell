
#!/usr/bin/env bash

#
# add_ubuntu_user_to_root_group
#
# Our main `ubuntu` user account needs to be able to do root-y stuff, like read
# and write directories that are owned by the `root` group; this script makes
# that possible
#

sudo usermod -a -G root ubuntu

